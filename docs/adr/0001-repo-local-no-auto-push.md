# Repo-Local No Auto-Push

This repository must not be automatically pushed to GitHub by agents, even when global or workspace policies would normally auto-sync idle local work. Local edits should be left unpushed and reported at task end; `git push` is allowed only after the user explicitly asks to push, sync, or publish this repo in the current conversation.
