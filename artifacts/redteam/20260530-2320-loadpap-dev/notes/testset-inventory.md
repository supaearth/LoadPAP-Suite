# Red-Team Testset Inventory

- Existing `testset/` directory: absent.
- Existing executable test files:
  - tests/test_utils.py
- LLM-calling tests planned for this run: 0, because no API keys or authenticated Google content should be transmitted during unattended DEV red-team.
- Non-LLM checks planned: Cloud Run health/root, Streamlit page route smoke, security header/CSP observations, responsive browser screenshots, console error capture.
